apiVersion: v1
kind: ConfigMap
metadata:
  name: t28bet-config
  namespace: t28bet
data:
  PORT: "3001"
  LOG_LEVEL: "info"
  AWS_REGION: "us-east-1"
  JWT_EXPIRES_IN: "24h"
  # Same-host ALB routing in production; local dev uses the CRA proxy.
  CORS_ORIGIN: "*"
