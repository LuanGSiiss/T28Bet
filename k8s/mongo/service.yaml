apiVersion: v1
kind: Service
metadata:
  name: mongo-svc
  namespace: t28bet
spec:
  selector:
    app: mongo
  ports:
    - port: 27017
      targetPort: 27017
  clusterIP: None  # headless service required for StatefulSet DNS
